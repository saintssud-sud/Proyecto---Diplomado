# SUPABASE (unico modo)

Este proyecto usa SOLO la parte final. No existe modo DEMO.

Ejecutar:
`flutter run --dart-define-from-file=config/local.json`

Usa `SupabaseRegistroRepository`.

Las pantallas siguen usando la interfaz `RegistroRepository`.
