# ==================== Flutter / Dart ====================
.dart_tool/
.packages
.pub/
build/
coverage/
.flutter-plugins-dependencies

# ==================== IDE ====================
.idea/
.vscode/
*.iml

# ==================== Python (scripts de documentación) ====================
.venv/
__pycache__/
*.pyc

# ==================== Credenciales y datos privados ====================
# NUNCA subir estos archivos. Usa las plantillas *.example.* como guía.

# Firebase (project_id + API keys de cliente)
android/app/google-services.json
lib/firebase_options.dart
# Metadata de la FlutterFire CLI: no tiene API keys, pero expone el project_id.
# El archivo local se conserva para que flutterfire configure lo reutilice.
firebase.json

# Firma de Android (keystores + passwords)
android/local.properties
android/key.properties
*.jks
*.keystore
*.p12

# Variables de entorno y llaves genéricas
.env
.env.*
!.env.example
*.pem
*.key
serviceAccountKey.json
firebase-adminsdk*.json

