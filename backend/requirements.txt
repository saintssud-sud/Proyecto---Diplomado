# Dependencias del backend de SIGVACH (servicio de la API)
#
# Instalación:      pip install -r requirements.txt
# Ejecución local:  uvicorn app.main:app --reload --port 8011
#                   (o el lanzador SIGVACH.bat, que usa el puerto 8011)
# En el despliegue: uvicorn app.main:app --host 0.0.0.0 --port $PORT --proxy-headers
# Documentación:    http://localhost:8011/docs
#
# El módulo de adquisición no declara dependencias aquí: vive en el firmware
# del dispositivo, no en este servicio.

# --- Verificadas con las pruebas y con el arranque real del servicio --------
# (83 casos automatizados y arranque del servidor con el comando de despliegue,
#  verificados el 15/09/2026)
fastapi==0.141.1
pydantic==2.13.5
pydantic-settings==2.15.0
uvicorn[standard]==0.52.4

# --- Necesaria en el despliegue, pendiente de verificar en local ------------
# firebase-admin: SDK con el que el backend accede a Cloud Firestore y verifica
#   los tokens de identidad. Requiere las credenciales de la cuenta de servicio,
#   que se reciben por variables de entorno y nunca están en el repositorio.
firebase-admin>=6.5
